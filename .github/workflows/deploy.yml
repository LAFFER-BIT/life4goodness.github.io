name: Deploy LeanCloud Version to GitHub Pages

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout代码
        uses: actions/checkout@v4

      - name: 🔧 生成LeanCloud配置文件
        run: |
          echo "⚙️ 正在生成config.js..."
          cat > config.js << 'EOF'
          window.APP_CONFIG = {
            production: {
              leancloud: {
                appId: "${{ secrets.LEANCLOUD_APP_ID }}",
                appKey: "${{ secrets.LEANCLOUD_APP_KEY }}",
                serverURL: "${{ secrets.LEANCLOUD_SERVER_URL }}"
              }
            }
          };
          EOF
          echo "✅ config.js生成完成"
          
      - name: 📝 验证配置文件
        run: |
          if [ -f "config.js" ]; then
            echo "✅ config.js 文件存在"
            cat config.js
          else
            echo "❌ config.js 文件不存在"
            exit 1
          fi
          
          if [ -f "index.html" ]; then
            echo "✅ index.html 文件存在"
          else
            echo "❌ index.html 文件不存在"
            exit 1
          fi

      - name: 🚀 部署到GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
          force_orphan: true
          commit_message: "🚀 Deploy: ${{ github.event.head_commit.message }}"
          
      - name: 🎉 部署完成
        run: |
          echo "=========================================="
          echo "🎊 部署成功！"
          echo "=========================================="
          echo "📱 访问地址: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
          echo "⏰ 部署时间: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "📝 提交信息: ${{ github.event.head_commit.message }}"
          echo "=========================================="
